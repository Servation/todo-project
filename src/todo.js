class Todo {
    constructor(title, description, dueDate, priority, project, todoID) {
        this.title = title
        this.description = description
        this.dueDate = dueDate
        this.priority = priority
        this.project = project
        this.todoID = todoID
    }
}

export default Todo;